apiVersion: v1
kind: ConfigMap
metadata:
  name: transfer-shortener-config
  namespace: default
data:
  LISTEN_ADDR: ":8080"
  BACKEND_URL: "http://transfer:5327"
  PUBLIC_URL: "https://transfer.sixtyfive.me"
  DB_PATH: "/data/shortener.db"
